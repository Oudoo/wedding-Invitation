# Antigravity Protocol

1. Read `.agent/AGENTS.md` for global rules — it is the single source of truth.
2. **CRITICAL:** Run `git fetch origin` before starting any vibe coding sequence.
3. Do not push to `main`. Push your work to a feature branch using `git push origin HEAD`.
4. Before concluding, update `.agent/HANDOFF.md` using the handoff template defined in `.agent/AGENTS.md`.
5. Commit frequently to preserve state across token boundaries.
